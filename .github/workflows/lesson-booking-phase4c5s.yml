name: Lesson booking Phase 4C5S

on:
  push:
    branches:
      - agent/lesson-booking-blueprint
    paths:
      - "scripts/lesson-booking-preview-notifier-proof-escalation-queue.mjs"
      - "scripts/check-lesson-booking-preview-notifier-proof-escalation-queue.mjs"
      - "scripts/lesson-booking-full-preview-supabase-transport.mjs"
      - "supabase/migrations/20260921081000_lesson_booking_phase4c5s_notifier_proof_escalation_queue.sql"
      - "supabase/tests/lesson_booking_preview_launch_blocker_notifier_proof_escalation_queue_scenarios.sql"
      - ".github/workflows/lesson-booking-phase4c5s.yml"
  pull_request:
    paths:
      - "scripts/lesson-booking-preview-notifier-proof-escalation-queue.mjs"
      - "scripts/check-lesson-booking-preview-notifier-proof-escalation-queue.mjs"
      - "scripts/lesson-booking-full-preview-supabase-transport.mjs"
      - "supabase/migrations/20260921081000_lesson_booking_phase4c5s_notifier_proof_escalation_queue.sql"
      - "supabase/tests/lesson_booking_preview_launch_blocker_notifier_proof_escalation_queue_scenarios.sql"
      - ".github/workflows/lesson-booking-phase4c5s.yml"

permissions:
  contents: read

jobs:
  notifier-proof-escalation-queue:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm install
      - name: Verify Phase 4C5S notifier proof and escalation queue boundary
        run: node scripts/check-lesson-booking-preview-notifier-proof-escalation-queue.mjs
      - name: Re-verify Phase 4C5R delivery and escalation boundary
        run: node scripts/check-lesson-booking-preview-launch-blocker-delivery-escalation.mjs
      - name: Start ephemeral PostgreSQL
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql postgresql-client
          sudo systemctl start postgresql
          sudo -u postgres pg_isready
      - name: Verify Phase 4C5S SQL behavior
        run: |
          set -euo pipefail
          sudo -u postgres psql -d postgres -v ON_ERROR_STOP=1 -f supabase/tests/bootstrap_minimal_supabase.sql
          for migration in supabase/migrations/*.sql; do echo "Applying ${migration}"; sudo -u postgres psql -d postgres -v ON_ERROR_STOP=1 -f "${migration}"; done
          sudo -u postgres psql -d postgres -v ON_ERROR_STOP=1 -f supabase/tests/lesson_booking_preview_launch_blocker_notifier_proof_escalation_queue_scenarios.sql
      - name: Build application
        run: npm run build
