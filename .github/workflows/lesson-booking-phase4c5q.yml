name: Lesson booking Phase 4C5Q

on:
  push:
    branches:
      - agent/lesson-booking-blueprint
    paths:
      - "scripts/lesson-booking-preview-launch-blocker-ack-handoff.mjs"
      - "scripts/check-lesson-booking-preview-launch-blocker-ack-handoff.mjs"
      - "scripts/lesson-booking-full-preview-supabase-transport.mjs"
      - "supabase/migrations/20260921063500_lesson_booking_phase4c5q_launch_blocker_ack_runbook_handoff.sql"
      - "supabase/tests/lesson_booking_preview_launch_blocker_ack_handoff_scenarios.sql"
      - ".github/workflows/lesson-booking-phase4c5q.yml"
  pull_request:
    paths:
      - "scripts/lesson-booking-preview-launch-blocker-ack-handoff.mjs"
      - "scripts/check-lesson-booking-preview-launch-blocker-ack-handoff.mjs"
      - "scripts/lesson-booking-full-preview-supabase-transport.mjs"
      - "supabase/migrations/20260921063500_lesson_booking_phase4c5q_launch_blocker_ack_runbook_handoff.sql"
      - "supabase/tests/lesson_booking_preview_launch_blocker_ack_handoff_scenarios.sql"
      - ".github/workflows/lesson-booking-phase4c5q.yml"

permissions:
  contents: read

jobs:
  launch-blocker-ack-runbook-handoff:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm install
      - name: Verify Phase 4C5Q acknowledgement and handoff boundary
        run: node scripts/check-lesson-booking-preview-launch-blocker-ack-handoff.mjs
      - name: Re-verify Phase 4C5P launch-blocker snapshot boundary
        run: node scripts/check-lesson-booking-preview-launch-blocker-snapshot.mjs
      - name: Start ephemeral PostgreSQL
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql postgresql-client
          sudo systemctl start postgresql
          sudo -u postgres pg_isready
      - name: Verify Phase 4C5Q SQL behavior
        run: |
          set -euo pipefail
          sudo -u postgres psql -d postgres -v ON_ERROR_STOP=1 -f supabase/tests/bootstrap_minimal_supabase.sql
          for migration in supabase/migrations/*.sql; do echo "Applying ${migration}"; sudo -u postgres psql -d postgres -v ON_ERROR_STOP=1 -f "${migration}"; done
          sudo -u postgres psql -d postgres -v ON_ERROR_STOP=1 -f supabase/tests/lesson_booking_preview_launch_blocker_ack_handoff_scenarios.sql
      - name: Build application
        run: npm run build
